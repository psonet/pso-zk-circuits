[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
license.workspace = true
publish = false
description = "Repo release tooling (canonical-circuit freezing). Not shipped."

[dependencies]
serde_json = { workspace = true }
base64 = { workspace = true }
tiny-keccak = { workspace = true }
toml_edit = { workspace = true }
