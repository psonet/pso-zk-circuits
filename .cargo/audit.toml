# Ignored advisories for transitive dependencies we cannot update.
# Review periodically and remove once upstream (noir, uniffi, halo2-axiom) updates.

[advisories]
ignore = [
    "RUSTSEC-2024-0436", # paste — unmaintained (halo2curves-axiom, uniffi, rmp)
    "RUSTSEC-2025-0057", # fxhash — unmaintained (noir)
    "RUSTSEC-2025-0141", # bincode — unmaintained (noir, uniffi)
]
