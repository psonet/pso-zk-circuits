// SPDX-License-Identifier: Apache-2.0
// Vendored from zkpassport/noir_rs (Apache-2.0).
// See `pso-zk-circuit-noir/vendor/noir_rs/LICENSE` for the original license.
//
// Upstream gates this on a `barretenberg` cargo feature; this crate
// always uses the barretenberg backend so the gate is omitted.

pub mod barretenberg;
