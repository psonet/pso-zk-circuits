[package]
name = "pso-zk-canonical"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Concrete canonical circuit/witness types for the PSO protocol (ownership + flat aggregation), built on the generic pso-protocol traits."
readme = "README.md"

[build-dependencies]
serde_json = { workspace = true }
tiny-keccak = { workspace = true }
base64 = { workspace = true }
toml = { workspace = true }

[dependencies]
pso-protocol = { workspace = true }
ark-ff = { workspace = true }
ark-std = { workspace = true }
ark-bn254 = { workspace = true }
# Compile-time `hex!("..")` -> `[u8; N]` for the generated circuit/vk hashes.
hex-literal = { workspace = true }
